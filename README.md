# LogLens

Turn mixed-format logs into a readable reconstruction of execution flow.

See `DESIGN.md` for the architecture and `BUILD_PLAN.md` for the V1 build steps.

## Install

```
pip install loglens
```

Optional semantic query extra:

```
pip install loglens[ai]
```
